// Sitemap generation is now handled by /scripts/generateSitemap.js during build time
const generateSitemap = () => {
  // No-op: Sitemap is now generated during build
};

export default generateSitemap;
